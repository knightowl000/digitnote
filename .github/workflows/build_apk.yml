name: Build Android APK

on:
  push:
    branches: [main]
  workflow_dispatch:  # 允许手动触发

jobs:
  build:
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v4

      - name: Setup build environment
        run: |
          sudo apt update
          sudo apt install -y python3-pip openjdk-17-jdk git autoconf libtool zip unzip
          pip install buildozer cython

      - name: Build APK
        env:
          JAVA_HOME: /usr/lib/jvm/temurin-17-jdk-amd64
        run: |
          # 清除可能冲突的 Android 环境变量，让 Buildozer 自己管理
          unset ANDROID_HOME ANDROID_SDK_ROOT ANDROID_SDK_HOME
          unset ANDROID_NDK ANDROID_NDK_HOME ANDROID_NDK_ROOT
          buildozer android debug

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: digitnote-apk
          path: bin/*.apk
